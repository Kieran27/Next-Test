import React from "react";

const Loading = () => {
  return (
    <div className="flex items-center justify-center text-center min-h-screen min-w-full">
      Loading...
    </div>
  );
};

export default Loading;
